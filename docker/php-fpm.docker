FROM php:7.1-fpm

RUN apt-get update && apt-get install -y libmcrypt-dev mysql-client zlib1g-dev libxrender1 libfontconfig1 libxext6 libpng-dev libldap2-dev \
    && docker-php-ext-install mcrypt pdo_mysql gd zip ldap

WORKDIR /var/www
